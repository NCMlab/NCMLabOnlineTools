# ConfigurationDatabase Design Discussion Log

> **Authorship notice**
> This document was generated by **Claude Code** (an AI coding assistant), starting
> **2026-07-07**. All SQL, code, and analysis prose were written by the AI. Jason Steffener
> directed the work by prompting, deciding, and correcting throughout.

> **How to read this log — provenance convention**
> Starting with the **2026-07-08 — Session Log** section below, every instruction Jason gave is
> quoted **verbatim, word-for-word (typos included)**, in a labeled blockquote:
> > **Jason's prompt:** "..."
>
> Nothing is paraphrased or cleaned up — if it looks like a typo, it is one, preserved exactly as
> typed. Text labeled **Claude:** is entirely the AI's own writing: analysis, design reasoning,
> code, and summaries of actions taken. Anywhere Jason chose between AI-presented options (via a
> multiple-choice prompt) rather than typing free text, that is labeled **Jason's choice** with
> the exact option text he selected.
>
> The **2026-07-07 content directly below** (Problem Statement through the first version of
> Table 5) predates this convention — it was produced in an earlier session, and the exact
> prompts that drove it were not preserved verbatim in this log. That section should be read as
> the AI's own synthesis of the task, not as a prompt transcript. Everything from **2026-07-08
> onward** is a complete, faithful prompt-by-prompt record.

**Started:** 2026-07-07  
**Location:** Office computer (jason@uottawa)  
**Repo branch:** DEVELOPMENT  

---

## Problem Statement

`Batteries/Batteries.js` specifies a list of batteries (study sessions). Each battery contains
an ordered list of tasks. Each task entry references:

| Field | Example | File it points to |
|---|---|---|
| `Task` | `"Word Recall"` | `html/JATOS/WordRecall.html` |
| `Parameters` | `"RAVLT_Spoken_Immediate"` | `config/WordRecall_Setup.js` (named JS object) |
| `Instructions` | `"Default"` | `InstructionsAndStimuli/Instructions/WordRecall_Instructions.js` |
| `IconName` | `"Immediate"` | Display label only |

This file-based approach has become bulky and hard to maintain. The goal is to move all
configuration into a MySQL database.

---

## Current System Inventory

### Battery-level fields (in Batteries.js)
```
index               – numeric ID used to look up the battery at runtime
name                – study identifier (e.g. "H-10-23-9755")
description         – short label
BatteryInstructions – welcome/intro text shown to participant
Language            – "EN" or "FR"
RunAudioTest        – boolean
Footer              – copyright string
shortName           – abbreviated lab name
HeaderButtonsToShow – array of button labels (e.g. ['Quit','Stop'])
LanguagesToShow     – optional array (e.g. ['EN','FR'])
Redirect            – URL to send participant after completion
TaskList            – ordered array of task entries (see below)
```

### Task entry fields
```
Task         – task type name (maps to an HTML file)
Parameters   – name of a parameter-set object in the task's config JS file
Instructions – name of an instruction-set object in the task's instructions JS file
IconName     – display name in the task-list UI
```

### Config files (one per task type, e.g. config/WordRecall_Setup.js)
Each file defines multiple named parameter objects. Parameters vary completely by task type
(a Word Recall parameter set looks nothing like a Trail Making parameter set).
Example fields for RAVLT_Spoken_Immediate:
```
FolderName, WordList, FileExtension, NBlocks, BListFlag,
AudioPresentation, VisualPresentation, RecallType, RecallDuration,
DelayBeforeShowingDoneButton, TimePerWord, DelayedRecallFlag,
ShowWelcome, WelcomeSpoken, WelcomeAudio, ShowThankYou,
ShowInstructions, InstructionsSpoken, AskForNotes, RecordAUDIO,
Instructions01Audio[], Instructions01Time[], ...
```

### Instruction files (one per task type, e.g. InstructionsAndStimuli/Instructions/WordRecall_Instructions.js)
Each file defines multiple named instruction objects, typically keyed by language.
Example fields for EN_Instructions_Default:
```
WelcomeText, RecallInstructions, GetReadyInstructions,
Instructions01..N (arrays of page objects),
WordPrompt, ThankYouText, IntrusionPrompt, WordRecallPrompt
```

---

## Database Design

### Design Decision: JSON Columns for Parameters and Instructions

Because each task type has a completely different parameter schema, storing parameters as
rows (key-value pairs) would require many sparse rows or a complicated EAV schema. Instead,
we store the full parameter object as a JSON column. MySQL 5.7.8+ supports native JSON.
The HTML task code already consumes these objects wholesale, so a JSON blob is a natural fit.

---

### Table 1: `task_types`
Catalog of all available task types. One row per task type.

```sql
CREATE TABLE task_types (
    task_type_id   INT           AUTO_INCREMENT PRIMARY KEY,
    task_name      VARCHAR(100)  NOT NULL UNIQUE,   -- e.g. "Word Recall"
    html_file      VARCHAR(255)  NOT NULL,           -- e.g. "html/JATOS/WordRecall.html"
    icon_file      VARCHAR(255),                     -- e.g. "assets/Icons/WordRecall.png"
    created_at     TIMESTAMP     DEFAULT CURRENT_TIMESTAMP
);
```

---

### Table 2: `task_parameters`
Each row is one named parameter set for one task type.

```sql
CREATE TABLE task_parameters (
    parameter_id    INT           AUTO_INCREMENT PRIMARY KEY,
    task_type_id    INT           NOT NULL,
    parameter_name  VARCHAR(150)  NOT NULL,   -- e.g. "RAVLT_Spoken_Immediate"
    language        CHAR(2)       DEFAULT 'EN',
    parameters_json JSON          NOT NULL,   -- full parameter object as JSON
    created_at      TIMESTAMP     DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uq_task_param (task_type_id, parameter_name),
    FOREIGN KEY (task_type_id) REFERENCES task_types(task_type_id)
);
```

---

### Table 3: `task_instructions`
Each row is one named instruction set for one task type.

```sql
CREATE TABLE task_instructions (
    instruction_id    INT           AUTO_INCREMENT PRIMARY KEY,
    task_type_id      INT           NOT NULL,
    instruction_name  VARCHAR(150)  NOT NULL,   -- e.g. "Default"
    language          CHAR(2)       DEFAULT 'EN',
    instructions_json JSON          NOT NULL,   -- full instruction object as JSON
    created_at        TIMESTAMP     DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uq_task_instr (task_type_id, instruction_name, language),
    FOREIGN KEY (task_type_id) REFERENCES task_types(task_type_id)
);
```

---

### Table 4: `batteries`
One row per battery (study session configuration).

```sql
CREATE TABLE batteries (
    battery_id            INT           AUTO_INCREMENT PRIMARY KEY,
    battery_index         INT           NOT NULL UNIQUE,   -- the numeric lookup key
    battery_name          VARCHAR(100)  NOT NULL,          -- e.g. "H-10-23-9755"
    description           VARCHAR(255),
    battery_instructions  TEXT,                            -- welcome text shown to participant
    language              CHAR(2)       DEFAULT 'EN',
    run_audio_test        TINYINT(1)    DEFAULT 0,
    footer                VARCHAR(255),
    short_name            VARCHAR(50),
    header_buttons        JSON,                            -- e.g. ["Quit","Stop"]
    languages_to_show     JSON,                            -- e.g. ["EN","FR"]
    redirect_url          VARCHAR(500),
    active                TINYINT(1)    DEFAULT 1,
    created_at            TIMESTAMP     DEFAULT CURRENT_TIMESTAMP
);
```

---

### Table 5: `battery_tasks`
Join table: ordered list of tasks within each battery.

```sql
CREATE TABLE battery_tasks (
    battery_task_id   INT           AUTO_INCREMENT PRIMARY KEY,
    battery_id        INT           NOT NULL,
    task_type_id      INT           NOT NULL,
    sort_order        INT           NOT NULL,   -- position in the task list (1-based)
    parameter_id      INT,                      -- FK to task_parameters (NULL if no params)
    instruction_id    INT,                      -- FK to task_instructions (NULL if no instr)
    icon_name         VARCHAR(100),             -- display label, e.g. "Immediate"
    FOREIGN KEY (battery_id)    REFERENCES batteries(battery_id),
    FOREIGN KEY (task_type_id)  REFERENCES task_types(task_type_id),
    FOREIGN KEY (parameter_id)  REFERENCES task_parameters(parameter_id),
    FOREIGN KEY (instruction_id) REFERENCES task_instructions(instruction_id)
);
```

---

### Table 6: `ui_labels`
Shared button/UI label text (e.g. "Quit", "Home", "Submit"), one row per (label_key, language)
pair. Added 2026-07-08 to hold the `LabelNames` objects from `config/General_Setup.js`, which
are used by every task's HTML page and therefore don't belong in `task_parameters` (they aren't
tied to any one `task_type_id`).

Originally designed as one JSON-blob row per language (matching the `task_parameters` pattern),
then revised the same day to one row per key: unlike `task_parameters`, every `ui_labels` row
shares the *same* set of keys across languages, which is exactly the case a normalized table
handles better. Concrete motivation: the blob design was already hiding a real bug — `FR` and
`KR` are both missing an `Info` key that `EN` has — invisible without diffing JSON by eye. With
one row per key it's a plain query:
```sql
SELECT label_key FROM ui_labels WHERE language='EN'
  AND label_key NOT IN (SELECT label_key FROM ui_labels WHERE language='FR');
```
`label_value` is JSON (not `VARCHAR`) because a few values are arrays, e.g. `NoYes: ["No","Yes"]`.

```sql
CREATE TABLE ui_labels (
    ui_label_id   INT           AUTO_INCREMENT PRIMARY KEY,
    label_key     VARCHAR(100)  NOT NULL,
    language      CHAR(2)       NOT NULL,
    label_value   JSON          NOT NULL,
    created_at    TIMESTAMP     DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uq_label_lang (label_key, language)
);
```

---

### Table 7: `session_chooser_configs`
Button-grid configuration for the session-picker screen (`config/SessionChooser_config.js`), one
row per named config per language (e.g. `COMM_99` in EN/FR/KR, `Mont_99` in FR). Added
2026-07-08 — Jason: "SessionChoose_config needs its own table." Not tied to `task_types`; this
isn't a task, it's a separate landing page whose buttons launch a battery (each `List` entry
carries a `battery` index).

Unlike `ui_labels`, kept as a JSON blob per row (like `task_parameters`) rather than normalized:
each config's `List` array genuinely has a different shape row to row (some entries carry
`Phase`/`Test`/`ButtonUsageType`, some don't), so there's no fixed set of columns to normalize
into — the same reasoning that justified JSON blobs for `task_parameters` in the first place.

`config_name` is the registration key (e.g. `EN_COMM_99`) with its language prefix stripped —
note the source data has one inconsistency worth knowing about: `FR_Mont_99` is registered under
an `FR` key but its `Title`/`List` text is actually English, not French. Migrated as-is rather
than silently corrected.

```sql
CREATE TABLE session_chooser_configs (
    session_chooser_config_id INT           AUTO_INCREMENT PRIMARY KEY,
    config_name                VARCHAR(100) NOT NULL,
    language                   CHAR(2)      NOT NULL DEFAULT 'EN',
    config_json                JSON         NOT NULL,
    created_at                 TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uq_session_chooser (config_name, language)
);
```

---

### Entity-Relationship Summary

```
task_types
  |-- task_parameters  (many parameter sets per task type)
  |-- task_instructions (many instruction sets per task type)

batteries
  |-- battery_tasks    (ordered list, one row per task slot in the battery)
        |-- task_types       (which task)
        |-- task_parameters  (which parameter set)
        |-- task_instructions (which instruction set)

ui_labels (standalone -- one row per label per language, referenced by every task's HTML page
           directly, not through a foreign key)

session_chooser_configs (standalone -- one row per named config per language; each List entry
           references a battery_index, not through a foreign key)
```

---

## SQL Commands to Create the Schema

See file: `CreateSchema.sql`

---

## How HTML Files Will Communicate with the Database

### Architecture: REST API Middleware

HTML study components (running in JATOS in the participant's browser) cannot connect directly
to MySQL — browsers have no MySQL driver. The standard solution is a thin REST API layer
running on the server that the HTML pages call with `fetch()`.

**Recommended stack:** PHP (already available on most Linux servers) or Node.js/Express.

#### Request flow:
```
Browser (JATOS study HTML)
    |
    | fetch('https://yourserver.ca/api/battery.php?index=16')
    v
REST API (PHP or Node.js on your server)
    |
    | SELECT * FROM batteries WHERE battery_index = 16
    | SELECT * FROM battery_tasks WHERE battery_id = ...
    | SELECT parameters_json FROM task_parameters WHERE ...
    | SELECT instructions_json FROM task_instructions WHERE ...
    v
MySQL database
    |
    v
API assembles JSON response, returns to browser
    |
    v
Browser JavaScript parses JSON, runs study as before
```

#### Example API response (replaces current BatteryList entry):
```json
{
  "battery_index": 16,
  "battery_name": "H-10-23-9755",
  "language": "EN",
  "run_audio_test": true,
  "footer": "©NCMLab 2025",
  "header_buttons": ["Quit","Stop"],
  "redirect_url": "https://...",
  "tasks": [
    {
      "task_name": "Word Recall",
      "html_file": "html/JATOS/WordRecall.html",
      "icon_name": "Immediate",
      "parameters": { ...RAVLT_Spoken_Immediate object... },
      "instructions": { ...EN_Instructions_Default object... }
    },
    ...
  ]
}
```

See files: `API_battery.php` and `API_example_fetch.js` for implementation.

---

## Files in This Folder

| File | Purpose |
|---|---|
| `DiscussionLog.md` | This file — running log of the design discussion |
| `ToDo.md` | Checklist: decisions needed from you, setup/deployment steps, remaining migration work |
| `AddNewLabel.md` | How-to: add one new UI label with EN/FR/KR translations |
| `AddNewLanguage.md` | How-to: add a whole new language to `ui_labels` |
| `CreateSchema.sql` | SQL to create database, all 8 tables, indexes, and view |
| `db_config.example.php` | Tracked template for DB credentials — copy to `db_config.php` (gitignored) and fill in real values |
| `SeedData_TaskTypes.sql` | INSERT for all 34 task types + full Word Recall example |
| `API_battery.php` | PHP endpoint returning a whole battery as JSON |
| `API_task_config.php` | PHP endpoint returning one parameter set or instruction set |
| `API_list_config.php` | PHP endpoint listing task types, or parameter/instruction sets for one task type (browsing, not fetching one known item) |
| `API_example_fetch.js` | Conceptual fetch() example (see WordRecallDatabaseConfig.html for real usage) |
| `SetupInstructions.md` | Step-by-step: MySQL setup, PHP deployment, API testing |
| `migrate/extract.js` | Node script: executes each config/instructions JS file in a vm sandbox and captures its `add()` registrations as JSON |
| `migrate/generate_sql.js` | Node script: turns `migrate/extracted/*.json` into `Migrated_TaskConfig.sql` + `MANUAL_REVIEW.md` |
| `migrate/extracted/*.json` | Raw extraction output, one file per source `.js` file |
| `migrate/Migrated_TaskConfig.sql` | Generated `INSERT IGNORE` statements: 227 task_parameters/task_instructions rows, 139 ui_labels rows, 7 session_chooser_configs rows |
| `migrate/MANUAL_REVIEW.md` | Everything the script could not place automatically, with reasons |
| `admin/admin_common.php` | Shared DB connection, session/CSRF, and CORS setup for every admin endpoint |
| `admin/API_admin_login.php` | Authenticates an `admin_users` account, starts a session, returns a CSRF token |
| `admin/API_admin_logout.php` | Destroys the current admin session |
| `admin/API_admin_whoami.php` | Checks the session cookie and reissues a CSRF token (used to restore login state after a page reload) |
| `admin/API_admin_create_parameter.php` | Authenticated: inserts one `task_parameters` row |
| `admin/API_admin_create_instruction.php` | Authenticated: inserts one `task_instructions` row |
| `admin/API_admin_create_battery.php` | Authenticated: inserts one `batteries` row + its `battery_tasks`, in a transaction |
| `admin/create_admin_user.php` | CLI-only script to create/update an admin login (no HTTP registration endpoint) |
| `admin/AdminSetupInstructions.md` | Deployment steps, request shapes, and known security gaps for the admin API |

## Task HTML Files (in html/JATOS/)

| File | Purpose |
|---|---|
| `WordRecall.html` | Original — loads config from JS files |
| `WordRecallDatabaseConfig.html` | New — fetches parameters and instructions from database API |

---

## Status

- [x] Problem statement defined (2026-07-07)
- [x] Current system inventoried (2026-07-07)
- [x] Database schema designed (2026-07-07)
- [x] CreateSchema.sql written (2026-07-07)
- [x] SeedData_TaskTypes.sql written — 27 task types + Word Recall example (2026-07-07)
- [x] PHP API written — API_battery.php + API_task_config.php (2026-07-07)
- [x] WordRecallDatabaseConfig.html created (2026-07-07)
- [x] SetupInstructions.md written (2026-07-07)
- [x] Migration script written and run against all 33 config files + 32 instruction files (2026-07-08)
- [x] All 10 previously-unmapped task types resolved — `task_types` grew from 27 → 34 rows (2026-07-08)
- [x] `ui_labels` and `session_chooser_configs` tables added for the 2 previously-unplaceable
      registration groups (2026-07-08)
- [x] 10 zero-`add()`-registration files sorted into `config/unused/`,
      `InstructionsAndStimuli/Instructions/unused/`, or left in place with their HTML page moved
      to `html/JATOS/unfinished/` (2026-07-08)
- [ ] `Migrated_TaskConfig.sql` reviewed and applied to a live database (227 + 139 + 7 INSERT
      statements, pending — see `ToDo.md` Part 2)
- [x] 7 pre-existing source bugs resolved — broken `add(...)` lines deleted from source
      (2026-07-08)
- [ ] `SelfReport_Setup.js` investigated — currently deferred, skipped entirely by
      `generate_sql.js` (2026-07-08)
- [ ] Word lists migrated (currently still loaded via pseudoSwitch from JS files)
- [ ] Battery records inserted for all existing batteries in Batteries.js
- [ ] Other task HTML files updated to use database config

---

## 2026-07-08 — Session Log (Prompt by Prompt)

> **Authorship notice:** this section generated by **Claude Code (model: claude-sonnet-5)**.
> See the provenance convention at the top of this document — every **Jason's prompt** block
> below is his exact, unedited text.

---

**Jason's prompt:**
> "Continue the MySQL ConfigurationDatabase design discussion from
> MySQL/ConfigurationDatabase/DiscussionLog.md"

**Claude:** Read this log and the existing schema/API/setup files to recover context, then asked
Jason which piece of the remaining work to tackle next.

**Jason's choice** (from a multiple-choice prompt): *"Migration script/approach"*

**Claude:** Built and ran an automated extractor for the two remaining bulk-migration items
(task parameters, task instructions) instead of hand-writing `JSON_OBJECT(...)` INSERTs as was
done for the one Word Recall example.

*Why not regex/text parsing:* the config/instruction files are not uniform — some are single
object literals (`var X = {...}`), some build the object via dot-assignment (`X.Field = ...`),
some alias one object to another (`CardSort_002 = CardSort_001`), some push onto arrays. Regex
parsing would need a special case per pattern and would silently mis-handle the rest. Instead
`migrate/extract.js` actually **executes** each file inside a Node `vm` sandbox, stubbing the
runtime's own `add(key, fn)` registration function (see
`functions/GeneralPurposeFunctions/pseudoSwitch.js`) to capture `{key, fn}` pairs without
running them yet, then calls each `fn` and reads back whichever conventional variable
(`parameters` or `Instructions`) it set — the exact mechanism `pseudoSwitch()` uses at runtime,
so the extracted value is guaranteed to match what the browser would actually load.

Two runtime dependencies had to be reproduced for this to work: every HTML page loads
`config/General_Setup.js` before a task's own files (defines shared globals like
`StimulusFontSize`, `LabelNames`) — the extractor now preloads it into the sandbox first; and a
few instruction files reference `BaseFolderName`, which pages set inline via
`<script>var BaseFolderName = '...'</script>` right before loading the instructions file —
harvested by hand per file into `BASE_FOLDER_NAME_BY_FILE` in `extract.js`.

**Result:** 227 of 241 registrations extracted cleanly and validated as JSON; 14 are
*pre-existing bugs* in the current `.js` source (an `add()` callback referencing a variable
never defined anywhere in that file — e.g. all 7 registrations in `SelfReport_Setup.js`
reference undefined `stai`/`nfc6`/`nfc10`/etc.). These would throw a `ReferenceError` today if
anything ever called `pseudoSwitch()` on them — the migration didn't create these gaps, it just
surfaced them.

`migrate/generate_sql.js` turns the clean extractions into `Migrated_TaskConfig.sql`
(`INSERT IGNORE`, so it's safe to re-run) by mapping each file's basename to a
`task_types.task_name`. 189 of the 227 had a confident mapping; the other 38 were written to
`MANUAL_REVIEW.md` rather than guessed at (10 files whose task type didn't exist in `task_types`
yet, and 10 registrations — `LabelNames`, `SessionChooser_config.js` — that are real data but
not any single task's parameter/instruction set).

---

**Jason's prompt:**
> "Create a To Do document. Include in it detailed instructions on what is needed for me to do.
> Also include the list of future steps hat are needed. When I complete a step , check off that
> I have done it. Do this when I either state that I completed it or you observe that it was
> completed."

**Claude:** Created `ToDo.md` with three parts — decisions only Jason can make, mechanical
setup/deployment steps, and remaining design/build work — and committed to keeping it checked
off as instructions above describe.

---

**Jason's prompt:**
> "Confirm vDMS → "Verbal DMS ... yes, this is correct"

**Claude:** Checked off the `vDMS → Verbal DMS` mapping in `ToDo.md` (had been a
medium-confidence guess up to this point).

---

**Jason's prompt:**
> "Add cancellation to task_types. Its htnl is html/JATOS/Cancellation.html"

**Claude:** Verified `html/JATOS/Cancellation.html` and `assets/Icons/Cancellation.png` both
exist, added `Cancellation` to `SeedData_TaskTypes.sql`, updated `generate_sql.js`'s mapping
table, and regenerated `Migrated_TaskConfig.sql` (189 → 194 INSERTs).

---

**Jason's prompt:**
> "Stroop is actually three tasks: Stroop Color, Stroop Word, Stroop Color/Word. They each have
> their own html file."

**Claude:** Added all three to `task_types` (`StroopColor.html`, `StroopWord.html`,
`StroopColorWord.html`, each with a matching icon). Discovered `Stroop_Setup.js`/
`Stroop_Instructions.js` are **one shared source file** for all three — each parameter/
instruction object already has combined `Color*`/`Word*`/`ColorWord*` fields, and each HTML page
reads only its own subset. Rather than splitting the extracted JSON three ways,
`generate_sql.js` now inserts the identical extracted JSON under all three `task_type_id`s.
Flagged a tradeoff: a future edit to shared Stroop content now needs 3 database writes instead
of 1 file edit. `Migrated_TaskConfig.sql`: 194 → 209.

---

**Jason's prompt:**
> "Make a new ui_labels table."

**Claude:** Built `ui_labels` as one JSON-blob row per language (`language`, `labels_json`),
matching the `task_parameters` pattern already used elsewhere in the schema. Wired
`generate_sql.js` to route the `LabelNames` registrations from `config/General_Setup.js` there.
Ended the turn by noting a real gap this surfaced — FR and KR are both missing an `Info` key
that EN has — and asked whether to fix it now or defer it.

---

**Jason's prompt:**
> "left for whenever you get to translations?"

**Claude:** Logged the FR/KR `Info` gap as a known, deliberately-deferred item in `ToDo.md`
rather than fixing it, per Jason's answer.

---

**Jason's prompt:**
> "Is that the best structure for the UI_labels? How do I add a new label in the three
> languages? How do I add all labels for a new language?"

**Claude:** Answered directly: unlike `task_parameters` (genuinely different schema per task),
every `ui_labels` row shares the *same* keys across languages, which is exactly the case a
normalized table suits better — and the blob design was already hiding the `Info` gap found in
the previous prompt, invisible without diffing JSON by eye. Presented both structures with SQL
examples and asked which to use.

**Jason's choice** (from a multiple-choice prompt): *"Switch to normalized (Recommended)"*

**Claude:** Rebuilt `ui_labels` as one row per `(label_key, language)` (`label_key`, `language`,
`label_value`, `UNIQUE(label_key, language)`). Updated `generate_sql.js` to emit one
`INSERT IGNORE` per label key (139 rows: 47 EN + 46 FR + 46 KR) instead of 3 blob rows. Verified
the redesign actually delivered the promised benefit by running the missing-translation query
against the regenerated SQL and confirming it found the known `Info` gap.

---

**Jason's prompt:**
> "try again"

**Claude:** Retried a `ToDo.md` edit that had been interrupted mid-write, checking off the
"where does LabelNames belong" decision with the final normalized design recorded.

---

**Jason's prompt:**
> "Provide detailed instructions on how to add a new label ands its translations into all three
> languages. Add detailed instructions on how to add a new language. Put these into new files."

**Claude:** Created two new files: `AddNewLabel.md` (add one label with EN/FR/KR translations)
and `AddNewLanguage.md` (add a 4th language to `ui_labels`). Both flag the same caveat:
`config/General_Setup.js` is still the *live* copy every HTML page actually reads
(`pseudoSwitch(LANG+'_LabelNames')`, used by ~50 pages) — `ui_labels` was, at this point, a
forward-looking mirror with no consumer yet, so both copies needed editing, not just the
database.

---

**Jason's prompt:**
> "SessionChoose_config needs its own table."

**Claude:** Read `config/SessionChooser_config.js` in full to understand its shape (button-grid
configs for the session-picker screen — a `Title` plus a `List` of buttons, each pointing at a
`battery` index). Built `session_chooser_configs`: one JSON-blob row per named config per
language, kept as a blob rather than normalized (unlike `ui_labels`) because each config's
`List` array genuinely varies in shape row to row. Routed the 7 registrations from
`SessionChooser_config.js` there. Side finding while migrating: `FR_Mont_99` is registered under
an `FR` key but its actual `Title`/`List` text is English — migrated as-is, flagged rather than
silently corrected. `Migrated_TaskConfig.sql`: 209 task rows + 139 ui_labels rows + 7
session_chooser_configs rows.

**Jason's prompt** (sent while the above was in progress):
> "Add to the ToDo list: use the ui_labels database table to replace use of the
> General_Setup.js"

**Claude:** Added a new `ToDo.md` item describing what that would take — a `ui_labels` API
endpoint plus updating every HTML page's label-loading logic (likely via a shared helper, given
~50 pages call `pseudoSwitch(LANG+'_LabelNames')`) — with `config/General_Setup.js`'s
`LabelNames` blocks only removable once every page is switched over.

---

**Jason's prompt:**
> "EQ5D, ImageCopy, ReadingListening are all real tasks that I want included"

**Claude:** Found that `EQ5D` had two candidate HTML files loading the identical
`EQ5D_Setup.js`/`EQ5D_Instructions.js` (`EQ5D.html` and `buEQ5D.html`, differing only in
header/footer include pattern), and asked Jason which to treat as canonical.

**Jason's choice** (from a multiple-choice prompt): *"EQ5D.html"*

**Claude:** Added `EQ5D`, `Image Copy`, and `Reading/Listening Test` to `task_types`. None of
the three have an icon in `assets/Icons/`, so `icon_file` is `NULL` for all three (same
convention as Email Entry/Ending Page/Form Questionnaire/Intake Form/Questionnaire).
`Reading/Listening Test` named with a slash to avoid colliding with the existing "Reading Test"
task type. Result: `MANUAL_REVIEW.md`'s unmapped-files section became empty — every extracted
registration now has somewhere to go except the 14 pre-existing source bugs.
`Migrated_TaskConfig.sql`: 209 → 227 task INSERTs; `task_types`: 31 → 34 rows.

---

**Jason's prompt:**
> "Is our conversation being logged in DiscussionLog.md?"

**Claude:** Answered honestly: no, not fully — this file had one dated entry that was already a
turn behind the actual conversation, while `ToDo.md` had become the more complete session-by-
session record. Offered to bring `DiscussionLog.md` back in sync.

**Jason's prompt:**
> "yes"

**Claude:** Added the two prose-narrative dated entries that (as of this rewrite) have now been
replaced by this prompt-by-prompt log, covering everything from the "Migration Script" work
through the EQ5D/ImageCopy/ReadingListening addition above.

---

**Jason's prompt:**
> "Make a folder called unused in the config folder. and put all of the files identified in
> "Decide fate of task-config files with zero add() registrations found at all " in it."

**Claude:** Before moving anything, checked whether any HTML page still referenced each of the
10 files — not just whether the extractor had found `add()` calls in them. Found a real gap in
the earlier manual-review process: 4 of the 10 (`ANT_Setup.js`, `SRT_Setup.js`,
`AttentionNetworkTest_Instructions.js`, `SRT_Instructions.js`) are still loaded by
`AttentionNetworkTest.html`/`SRT.html` via plain `<script src>` — they just don't use the
`add()`/`pseudoSwitch()` pattern, so "zero registrations found" didn't mean "unused" for these.
Moved the other 6, which genuinely have no references anywhere, into a new `config/unused/`, and
asked how to handle the directory-mismatch (5 of the 10 files actually live in
`InstructionsAndStimuli/Instructions/`, not `config/`) and the 4 live-but-flagged files.

**Jason's choice** (from a multiple-choice prompt, question 1): *"InstructionsAndStimuli/Instructions/unused/"*

**Jason's answer** (question 2, free text):
> "Move the two html files into a folder called unfinished. Thse are projects that have not been
> completed yet."

**Claude:** Moved `AFT_Instructions.js`, `ICAR_MatrixReasoning_Instructions.js`,
`ReyComplexFigure_Instructions.js` into a new `InstructionsAndStimuli/Instructions/unused/`
(mirroring their original directory rather than dumping into `config/unused/`). Left the 4
live-but-extractor-invisible `.js` files in place, and instead moved their HTML entry points,
`AttentionNetworkTest.html` and `SRT.html`, into a new `html/JATOS/unfinished/`. Flagged an
unresolved caveat: both HTML files use paths relative to `html/JATOS/` with no `../` prefix, so
moving them one directory deeper broke every relative script/asset path in both files — not
fixed, since Jason didn't ask for that and both are already marked unfinished, but noted in
`ToDo.md` for whoever resumes either task.

---

**Jason's prompt:**
> "In the DiscussionLog.md file make it more clear what my prompts are. I want to be perfectly
> clear what my instructions and responses are. This md file will accompany the code to
> demonstrate how I used AI in teh construction of the code."

**Claude:** Added the provenance convention note at the top of this document and rewrote this
entire "2026-07-08" section (previously two prose paragraphs that paraphrased Jason's prompts
inline, and which had accidentally duplicated one paragraph) into the prompt-by-prompt transcript
you are reading now — every **Jason's prompt** block quoted verbatim, every **Claude:** block
Claude's own writing.

**Jason's prompt** (sent while the above was in progress):
> "Mark this complete "Decide fate of task-config files with zero add() registrations found at
> all" in teh ToDo.md file"

**Claude:** Already checked off during the `unused`/`unfinished` file-move work two prompts
above — confirmed the checkbox state in `ToDo.md` rather than re-doing it.

---

**Jason's prompt:**
> "From the Manual_review.md ... ignore the file SelfReport_Setup.js. I am not sure what that is
> yet."

**Claude:** Added a `SKIP_FILES` exclusion to `generate_sql.js` so `SelfReport_Setup.js` is
skipped entirely during extraction/SQL generation — not reported as 7 broken registrations
(all 7 of its `add()` callbacks reference undefined variables, so it contributes zero valid data
either way), just set aside pending Jason's own investigation into what the file actually is.
Gave `MANUAL_REVIEW.md` a new "Files explicitly deferred (not processed)" section, distinct from
the "pre-existing bugs" section, so the distinction between "confirmed broken" and "not yet
understood" stays visible rather than being flattened into one bucket. Regenerated
`Migrated_TaskConfig.sql` (unchanged, since this file contributed nothing valid) and confirmed
`MANUAL_REVIEW.md`'s pre-existing-bugs count dropped from 14 to 7. Updated `ToDo.md`'s
corresponding item to match, and added a new item to actually resolve what `SelfReport_Setup.js`
is.

---

**Jason's prompt:**
> "Remove the add('key',...) calls that reference a variable never defined."

**Claude:** Deleted the 7 broken `add(...)` lines directly from the source `.js` files (not just
from the migration output — this is a real code change to `config/`, unlike the SelfReport
deferral above). One line removed from each: `DigitSpan_Setup.js`
(`DigitSpan_Audio_Backward_Fixed01_Load03`), `Fluency_Setup.js` (`Fluency_TEST`),
`ImageCopy_Setup.js` (`ImageCopy_FaCE`), `Stroop_Setup.js` (`Stroop_001`),
`TrailMaking_Setup.js` (`TrailMakingB_DefaultTEST`); two lines from
`SerialSubtraction_Setup.js` (`SerialSubtract_FaCE`, `SerialSubtract_FaCE_02` — both referenced
the same undefined variable). Left the surrounding code untouched, including a stale section
comment in `DigitSpan_Setup.js` (`// == DigitSpan_Audio_Backward_Fixed01_Load03 ==`) that marks
where the removed registration's variable was apparently meant to be — that comment is
pre-existing documentation, not a broken registration, so it was out of scope for this request.
Re-ran `extract.js` + `generate_sql.js` to confirm: `MANUAL_REVIEW.md`'s "Pre-existing bugs"
section is now empty, and `Migrated_TaskConfig.sql` is unchanged (227/139/7 INSERTs — these
registrations never produced valid data, so removing them lost nothing).

---

**Jason's prompt:**
> "I have another repository named NCMBatteryWebsite which is a ReactJS UI for creating Battery
> configurations like in teh Batteries/Batteries.js file. I would like to use this UI for making
> entries into the dB that was just created."

*(Interrupted and re-sent by Jason with more detail:)*

**Jason's prompt:**
> "I have another repository named NCMBatteryWebsite which is a ReactJS UI for creating Battery
> configurations like in the Batteries/Batteries.js file. I would like to use this UI for making
> entries into the dB that was just created. The users that will be using this UI will be
> privileged used, not the general publiuc, so I can use a login and passowrd for validation of
> them. I believe this is an issue since I want any writing to teh dB to be secure. How do I
> include this other repo into my primary repo (the current one)?"

**Claude:** Located `NCMBatteryWebsite` locally (`Documents/GitHub/NCMBatteryWebsite`) and
inspected it: a Create React App project whose git remote points to a different GitHub account
(`Keynaan-I`), with `node_modules` accidentally tracked in git (no `.gitignore`, ~38,800 files).
Rather than assume how to combine the two repos, asked three questions: how to connect them at
the repo level, whether to fix the `node_modules` tracking issue, and whether the write-access
login should be a new system or reuse an existing one.

**Jason's choices** (all three from multiple-choice prompts):
- Repo integration: *"Keep them separate repos (Recommended)"* — no git-level merge; the React
  app stays its own repo and talks to a new authenticated PHP API over HTTP, the same pattern
  the existing read-only `API_battery.php` already uses.
- `node_modules` cleanup: *"Yes, clean it up (Recommended)"*
- Auth design: *"Design a fresh login table (Recommended)"*

**Claude:** Added a `.gitignore` to `NCMBatteryWebsite/user-website/` and ran
`git rm -r --cached node_modules`, committing locally (38,809 files changed, not pushed — that
remote belongs to a different account, so pushing is Jason's call). Then read
`NCMBatteryWebsite`'s actual source (`App.js`, `Home.js`, `FormSettings.js`) before designing
anything further, and found the UI is currently a client-only prototype: "Save as Battery"
downloads a `battery.json` file via `localStorage`, there is no backend call anywhere in the
app yet, the task list is hardcoded, and `FormSettings.js` reads default parameters from a
frozen copy of the old `config/*.js` files (`src/configFiles/`) that already disagrees with the
database (different naming, missing the Cancellation/Stroop/EQ5D/etc. additions from earlier in
this log). Reported this and asked one more question: should the write API only let the UI
assemble batteries from *existing* task types/parameters/instructions, or also let it create
*new* parameter/instruction sets.

**Jason's choice:** *"Batteries + new parameter/instruction sets"*

**Jason's prompt** (sent while the above was being designed):
> "pause, I need to restart my computer"

**Claude:** Confirmed both repos were in a clean, fully-committed state (nothing left
uncommitted or half-done) and stopped.

**Jason's prompt:**
> "continue"

**Claude:** Built the authenticated write API. Added `admin_users` to `CreateSchema.sql` as
Table 8 (`username`, bcrypt `password_hash`, `active`) — no self-registration endpoint, since
these are privileged accounts, not public sign-up. Built 6 new files under a new `admin/`
folder, deliberately separate from the existing public `API_battery.php`/`API_task_config.php`
(which stay unauthenticated and wide-open CORS, appropriate for study participants' browsers):

- `admin_common.php` — shared DB connection, PHP session config (`httpOnly`, `secure`,
  `SameSite=Strict` cookie), CORS locked to one explicit origin (not `*` — browsers reject
  wildcard CORS on credentialed requests), and a `require_admin_session()` check every write
  endpoint calls first (verifies both an active session *and* a CSRF token sent via
  `X-CSRF-Token`, checked with `hash_equals()` to avoid timing attacks).
- `API_admin_login.php` — `password_verify()` against the bcrypt hash, identical error message
  for "no such user" and "wrong password" (distinguishing them would let an attacker enumerate
  valid usernames), `session_regenerate_id(true)` on success to block session fixation, issues a
  fresh CSRF token.
- `API_admin_logout.php` — destroys the session.
- `API_admin_create_parameter.php` / `API_admin_create_instruction.php` — insert one
  `task_parameters`/`task_instructions` row each; validate the name against
  `^[A-Za-z0-9_]+$`, resolve `task_type_id` or `task_name` to a real row first, and return 409
  rather than silently overwriting if that (task_type, name) pair already exists — there's no
  update endpoint yet, so overwriting-by-default would be a dangerous behavior for a name other
  batteries may already reference.
- `API_admin_create_battery.php` — inserts one `batteries` row plus its `battery_tasks` in a
  single transaction (all-or-nothing), and cross-checks that any `parameter_id`/`instruction_id`
  supplied for a task actually belongs to that task's `task_type_id` (not just that the ID
  exists at all — a valid Word Recall parameter set attached to a Trail Making task slot would
  pass a plain foreign-key check but be logically wrong).
- `create_admin_user.php` — CLI-only (checks `php_sapi_name() !== 'cli'` and refuses to run
  under a web server even if this file ends up somewhere web-accessible), prompts for a
  password without echoing it, requires 12+ characters, hashes with `password_hash()`.
- `AdminSetupInstructions.md` — deployment steps, the exact request/response shape the React
  app needs to call, and an explicit "known gaps" section: no login rate-limiting, no audit log
  of which admin created what, no update/delete endpoints (scope decision, not an oversight) —
  documented rather than silently shipped as if complete.

Updated `ToDo.md` with a new "Part 2b" covering deployment of this API and wiring
`NCMBatteryWebsite` to it (including pointing its task/parameter lists at the live read API
instead of the stale `src/configFiles/` copy). None of this has been deployed or connected to
the actual React app yet — it's the API surface only.

---

**Jason's prompt:**
> "Do it: 'Wiring the React app to actually call this new API is the next piece of work
> whenever you're ready for it.'"

**Claude:** Wired `NCMBatteryWebsite` to the admin API built in the previous entry. First added
one more server-side piece the design had missed: `API_list_config.php` (public, unauthenticated
— a "list what exists" endpoint; the earlier `API_task_config.php` only supports "fetch one
already-known-by-name thing," which isn't enough for a UI that needs to *populate a picker*) and
`admin/API_admin_whoami.php` (lets the React app recover login state and a fresh CSRF token
after a page reload, since the in-memory CSRF token from login doesn't survive one).

On the React side, added `src/api.js` (a client for both the public read API and the admin write
API), `src/AuthContext.js` + `src/Login.js` (a login gate wrapping the whole app, using
`credentials: 'include'` so the session cookie flows automatically), and rewrote `Home.js` and
`FormSettings.js`:
- `Home.js`'s task grid now comes from `API_list_config.php?resource=task_types` instead of a
  hardcoded list of 20 ad-hoc names (some of which, like "Trails A"/"Trails B" or "DS,
  Forward"/"DS, Backwards", didn't correspond to the database's task-type granularity at all —
  the database models "Trail Making" and "Digit Span" as one task type each, with the
  Forward/Backward/A/B distinction living at the parameter-set level instead).
- "Save as Battery" now `POST`s to `API_admin_create_battery.php` instead of downloading a
  `battery.json` file.
- `FormSettings.js` no longer imports frozen default objects from `src/configFiles/` (which was
  already out of sync with the database — different naming, missing recent additions). It now
  lets the user either pick an existing parameter set for that task type (loaded read-only, since
  there's no update endpoint) or build a new one and save it via
  `API_admin_create_parameter.php`.
- Deleted `src/configFiles/` entirely — nothing referenced it anymore.

Two things found and fixed along the way, not part of the original ask but directly blocking or
adjacent to it: `uuid` was imported in `App.js`/`Home.js` but never listed in `package.json` —
it only worked because some other dependency happened to hoist it into `node_modules`, a latent
bug that would break on a clean install; added it as an explicit dependency. Also
`NCMBatteryWebsite/README.md` turned out to be a corrupted UTF-16 file containing only the repo
name — replaced with real setup instructions, since this work introduced real setup steps
(`.env.local` variables) worth documenting.

Verified with `npm run build` (`CI=true npx react-scripts build`) — compiles cleanly, no syntax
or missing-import errors. Could not verify against a live backend (no PHP server running
locally), so this is confirmed to *build*, not confirmed to *work end-to-end* against a real
database yet.

One scope gap surfaced during this work: the original UI never had any concept of instruction
sets (only "settings"/parameters), so batteries built through this UI currently save with
`instruction_id` left `null` — the schema allows it, but picking an instruction set per task
would need its own design pass, not an assumption bundled into this one.

---

**Jason's prompt:**
> "What is the login and password for the ReactJS site?"

**Claude:** Answered directly: no account exists yet anywhere — `create_admin_user.php` has
never been run, so `admin_users` has zero rows, and nothing has been deployed. Explained the CLI
command to create the first one, and that Claude neither sets nor knows the credentials (bcrypt
hash only).

**Jason's prompt:**
> "I get this response: Database connection failed: SQLSTATE[HY000] [1045] Access denied for
> user 'your_db_user'@'localhost' (using password: YES)"

**Claude:** Diagnosed: `create_admin_user.php` still had the literal placeholder
`define('DB_USER', 'your_db_user')` from when it was first written — expected, not a real
failure. Checked locally (a `mysql` client exists, a server is reachable, but no
`ncmbattery_config` database or dedicated user exists yet — couldn't check further without root's
password, which Claude didn't ask for). Gave Jason the exact `CREATE USER`/`GRANT` SQL to run
himself interactively, and offered to fill in `DB_USER` once he'd picked one — deliberately
suggesting Jason edit `DB_PASS` himself rather than paste it into the conversation.

**Jason's prompt:**
> "Ideally, the login and password should not be in the code. If I add my credentials and push
> to github, that is bad. Update the SetupInstructions for using credentials in a manner that
> does not risk them going to github."

**Claude:** Restructured how every PHP file gets its DB credentials, rather than just updating
docs around the existing pattern. Created `db_config.example.php` (tracked, placeholder values
only) and added `MySQL/ConfigurationDatabase/db_config.php` to the root `.gitignore` — verified
with `git check-ignore -v` that it actually takes effect, not just assumed. Replaced the inline
`define('DB_HOST', ...)` block in all 5 PHP files that had one
(`API_battery.php`, `API_task_config.php`, `API_list_config.php`, `admin/admin_common.php`,
`admin/create_admin_user.php`) with `require_once __DIR__ . '/db_config.php'` (or `/../db_config.php`
for the two files in `admin/`). Created a local `db_config.php` pre-filled with the same
placeholders (safe, since it's gitignored) for Jason to edit directly with real values — the
same "don't paste secrets into the conversation" approach from the previous prompt.

Updated both setup docs to match: `SetupInstructions.md` §2.3 changed from "edit credentials in
each file" to "copy `db_config.example.php` once per deployment location"; added a new
"Part 1.5" to `admin/AdminSetupInstructions.md` (had to slot in *before* Part 2's
`create_admin_user.php` step, since that script now depends on `db_config.php` existing first),
and trimmed Part 3's now-redundant credential instructions down to just the one setting that
genuinely isn't a secret (`ADMIN_ALLOWED_ORIGIN`). Confirmed no file outside the gitignored
`db_config.php` and the intentional troubleshooting example in
`AdminSetupInstructions.md` still mentions the placeholder value.

---

**Status as of the end of this log:** the database schema/SQL work (`ToDo.md` Part 2) and the
admin API deployment (`ToDo.md` Part 2b) both remain undeployed — everything so far has been
built and verified locally, but nothing has touched a live server. `NCMBatteryWebsite` is now
wired to call the admin API but has not been run against a real backend. DB credentials are no
longer at risk of being committed (`db_config.php` is gitignored), but Jason still needs to fill
in real values in his local `db_config.php` before `create_admin_user.php` or any API endpoint
will connect successfully. Remaining open items: `SelfReport_Setup.js` is deferred pending
investigation into what it is; the FR/KR `Info` translation gap is deferred by Jason's own
choice until translation work happens; the broken relative paths in the two
moved-but-unfinished HTML files are unresolved; the admin API's known gaps (rate limiting, audit
log, update/delete endpoints) are undone by design, not by accident; there is no instruction-set
picker in the UI yet.
See `ToDo.md` for the authoritative, currently-live checklist.

---

## How to Continue This Discussion from Another Computer

1. **On this computer:** commit and push this file:
   ```bash
   git add MySQL/ConfigurationDatabase/
   git commit -m "Start MySQL ConfigurationDatabase design discussion"
   git push
   ```

2. **On your home computer:** pull the repo:
   ```bash
   git pull
   ```

3. **Start a new Claude Code session** in the NCMBattery directory and say:
   > "Continue the MySQL ConfigurationDatabase design discussion from MySQL/ConfigurationDatabase/DiscussionLog.md"

   Claude will read that file and have full context of where we left off.

---
---
*Generated and maintained by Claude Code — model: claude-sonnet-4-6 — 2026-07-07*
