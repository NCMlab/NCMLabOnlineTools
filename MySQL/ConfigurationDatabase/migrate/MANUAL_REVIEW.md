# Migration: Manual Review

Generated by `generate_sql.js`. 189 INSERT statements were written to `Migrated_TaskConfig.sql`.

## Files with no matching task_types row

These config/instructions files have registrations that were extracted cleanly, but there is
no row in `task_types` to attach them to yet. Either the task type needs to be added to
`SeedData_TaskTypes.sql` (with its HTML file), or the task is deprecated/unused and these
files can be left out of the migration.

- Cancellation_Instructions.js
- EQ5D_Instructions.js
- ImageCopy_Instructions.js
- ReadingListening_Instructions.js
- Stroop_Instructions.js
- Cancellation_Setup.js
- EQ5D_Setup.js
- ImageCopy_Setup.js
- ReadingListening_Setup.js
- Stroop_Setup.js

## Registrations excluded as non-task-specific

These are real, cleanly-extracted values, but they are shared/global config or jsPsych
timeline fragments rather than one task's parameter/instruction set, so they do not belong
in `task_parameters`/`task_instructions` as currently designed. Needs a design decision on
where they should live (e.g. a `ui_labels` table for LabelNames, or leaving them as JS).

- **General_Setup.js** `EN_LabelNames` — General_Setup.js defines shared/global config, not a single task's parameters
- **General_Setup.js** `FR_LabelNames` — General_Setup.js defines shared/global config, not a single task's parameters
- **General_Setup.js** `KR_LabelNames` — General_Setup.js defines shared/global config, not a single task's parameters
- **SessionChooser_config.js** `EN_COMM_01` — SessionChooser_config.js defines shared/global config, not a single task's parameters
- **SessionChooser_config.js** `FR_COMM_01` — SessionChooser_config.js defines shared/global config, not a single task's parameters
- **SessionChooser_config.js** `EN_COMM_DEMO` — SessionChooser_config.js defines shared/global config, not a single task's parameters
- **SessionChooser_config.js** `EN_COMM_99` — SessionChooser_config.js defines shared/global config, not a single task's parameters
- **SessionChooser_config.js** `FR_COMM_99` — SessionChooser_config.js defines shared/global config, not a single task's parameters
- **SessionChooser_config.js** `KR_COMM_99` — SessionChooser_config.js defines shared/global config, not a single task's parameters
- **SessionChooser_config.js** `FR_Mont_99` — SessionChooser_config.js defines shared/global config, not a single task's parameters

## Pre-existing bugs found in the source (not caused by migration)

Each of these calls `add('key', ...)` with a callback that references a variable never
defined anywhere in the file. These are dead/broken registrations in the *current* codebase —
pseudoSwitch(key) would throw a ReferenceError at runtime today if anything ever selected them.

- **DigitSpan_Setup.js** `DigitSpan_Audio_Backward_Fixed01_Load03` — callback threw: DigitSpan_Audio_Backward_Fixed01_Load03 is not defined
- **Fluency_Setup.js** `Fluency_TEST` — callback threw: Fluency_TEST is not defined
- **ImageCopy_Setup.js** `ImageCopy_FaCE` — callback threw: ImageCopy_FaCE is not defined
- **SelfReport_Setup.js** `stai` — callback threw: stai is not defined
- **SelfReport_Setup.js** `nfc6` — callback threw: nfc6 is not defined
- **SelfReport_Setup.js** `nfc10` — callback threw: nfc10 is not defined
- **SelfReport_Setup.js** `nfcs` — callback threw: nfcs is not defined
- **SelfReport_Setup.js** `panas` — callback threw: panas is not defined
- **SelfReport_Setup.js** `rtq` — callback threw: rtq is not defined
- **SelfReport_Setup.js** `dass` — callback threw: dass is not defined
- **SerialSubtraction_Setup.js** `SerialSubtract_FaCE` — callback threw: SerialSubtract_FaCE is not defined
- **SerialSubtraction_Setup.js** `SerialSubtract_FaCE_02` — callback threw: SerialSubtract_FaCE is not defined
- **Stroop_Setup.js** `Stroop_001` — callback threw: Stroop_001 is not defined
- **TrailMaking_Setup.js** `TrailMakingB_DefaultTEST` — callback threw: TrailMakingB_DefaultTEST is not defined
