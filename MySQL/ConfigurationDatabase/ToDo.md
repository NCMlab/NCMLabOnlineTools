# ConfigurationDatabase — To Do

> **Authorship notice**
> This document was generated by **Claude Code (model: claude-sonnet-5)** on **2026-07-08**.

Tracks everything left before the MySQL configuration database replaces the current file-based
config. Checked off when you say a step is done, or when I observe it done (e.g. a file exists,
a table has rows). See `DiscussionLog.md` for the full design history behind these items.

---

## Part 1 — Decisions only you can make

These are judgment calls about the lab's data, not things I can resolve by reading code.
Details and the underlying evidence are in `migrate/MANUAL_REVIEW.md`.

- [x] **Confirm `vDMS` → "Verbal DMS".** `generate_sql.js` mapped `vDMS_setup.js` /
      `vDMS_Instructions.js` to the existing `task_types` row "Verbal DMS" by guessing from the
      name. Medium confidence only — please confirm this is correct (vs. e.g. a second spatial
      DMS variant) before the generated SQL is applied. — **Confirmed correct by Jason, 2026-07-08.**

- [x] **Decide fate of the remaining task types with no `task_types` row yet.** For each one,
      chose: **(a)** add it to `task_types` (needs a display name + its HTML file path) so its
      parameters/instructions can be migrated, or **(b)** it's deprecated/unused — leave it out.
      All resolved 2026-07-08 — no unmapped files remain (`MANUAL_REVIEW.md`'s "Files with no
      matching task_types row" section is now empty).
      - [x] ~~`EQ5D`, `ImageCopy`, `ReadingListening`~~ — Jason: "all real tasks that I want
        included." Added to `task_types`: `EQ5D` (`html/JATOS/EQ5D.html` — Jason picked this
        over `buEQ5D.html`, which loads the identical config/instructions but uses a different
        header/footer include pattern; `buEQ5D.html` is unused by the migration), `Image Copy`
        (`html/JATOS/ImageCopy.html`), `Reading/Listening Test`
        (`html/JATOS/ReadingListeningTest.html`) — named to avoid colliding with the existing
        "Reading Test" task type. None of the three have an icon file in `assets/Icons/`, so
        `icon_file` is `NULL` for all three, same convention as Email Entry/Ending Page/Form
        Questionnaire/Intake Form/Questionnaire. `Migrated_TaskConfig.sql` regenerated: 209 →
        227 task INSERTs.
      - [x] ~~`Cancellation`~~ — added to `task_types` 2026-07-08 (`html/JATOS/Cancellation.html`,
        `assets/Icons/Cancellation.png`). `Migrated_TaskConfig.sql` regenerated: 189 → 194
        INSERTs.
      - [x] ~~`Stroop`~~ — is actually **three** task types (per Jason, 2026-07-08), each with its
        own HTML file: `Stroop Color` (`StroopColor.html`), `Stroop Word` (`StroopWord.html`),
        `Stroop Color/Word` (`StroopColorWord.html`), all added to `task_types`. `Stroop_Setup.js`
        / `Stroop_Instructions.js` turned out to be **one shared file** used by all three pages
        (each parameter/instruction object has combined `Color*`/`Word*`/`ColorWord*` fields, and
        each HTML page only reads its own subset) — `generate_sql.js` now inserts the identical
        extracted JSON under all three task_type_ids rather than splitting it. Note: this means a
        future edit to shared Stroop content needs to be applied in 3 places in the database,
        unlike today's single shared file — worth keeping in mind if these ever need editing via
        the DB instead of regenerating from source. `Migrated_TaskConfig.sql` regenerated: 194 →
        209 INSERTs.

- [x] **Decide fate of task-config files with *zero* `add()` registrations found at all**
      (not even attempted by the extractor, because they don't use the `add()` pattern the same
      way). Resolved 2026-07-08: before moving anything, checked whether any HTML page still
      references each file — 6 had zero references anywhere and were genuinely unused; the other
      4 turned out to still be loaded by `AttentionNetworkTest.html`/`SRT.html` (they just don't
      use `add()`/`pseudoSwitch()`, which is why the extractor found nothing — that's a gap in
      the extractor's detection, not evidence of dead code).
      - [x] Moved to `config/unused/`: `BGR_Setup.js`, `Button_config.js`, `cfi_Setup.js`
        (zero references anywhere).
      - [x] Moved to `InstructionsAndStimuli/Instructions/unused/`: `AFT_Instructions.js`,
        `ICAR_MatrixReasoning_Instructions.js`, `ReyComplexFigure_Instructions.js` (zero
        references anywhere; Jason chose to mirror each file's original directory with its own
        `unused/` subfolder rather than dumping everything into `config/unused/`).
      - [x] `ANT_Setup.js`, `SRT_Setup.js`, `AttentionNetworkTest_Instructions.js`,
        `SRT_Instructions.js` — **left in place**, not moved. Jason: these are unfinished
        projects, not dead ones, so instead moved their HTML entry points,
        `AttentionNetworkTest.html` and `SRT.html`, to `html/JATOS/unfinished/`.
        **Caveat, not yet fixed:** both HTML files use paths relative to `html/JATOS/` (e.g.
        `<script src="config/ANT_Setup.js">`, no `../`), so moving them one directory deeper
        broke every relative script/asset path in both files. Left as-is since Jason didn't ask
        for a fix and these are already flagged unfinished — but anyone resuming either task
        needs to add `../` to every relative path in both files first (or move them back a level).

- [x] **Decide where `LabelNames` (EN/FR/KR) belongs.** Currently defined in
      `config/General_Setup.js` and shared by every task (button labels like "Quit", "Home",
      "Submit"). It isn't a single task's parameter set, so it doesn't fit `task_parameters` as
      designed. Options: a new `ui_labels` table, or leave it as a shared JS file outside the
      database. Affects 3 registrations (`EN_LabelNames`, `FR_LabelNames`, `KR_LabelNames`). —
      **Resolved 2026-07-08: new `ui_labels` table**, one row per `(label_key, language)` pair
      (`label_key` VARCHAR, `language` CHAR(2), `label_value` JSON, `UNIQUE(label_key,
      language)`), added to `CreateSchema.sql` as Table 6. Revised same day from an initial
      one-JSON-blob-per-language design after Jason asked "is that the best structure?" — the
      row-per-key version makes adding/editing a single label a plain INSERT/UPDATE instead of a
      `JSON_SET`, and surfaced a real bug: `FR` and `KR` are both missing an `Info` key that `EN`
      has. `generate_sql.js` now emits one `INSERT IGNORE INTO ui_labels` per label key (139
      rows: 47 EN + 46 FR + 46 KR) instead of 3 JSON-blob rows.
      `Migrated_TaskConfig.sql` regenerated accordingly. Not yet applied to a live database
      (Part 2 setup).

- [x] **Decide where `SessionChooser_config.js` page-config belongs** (7 registrations:
      `EN_COMM_01`, `FR_COMM_01`, `EN_COMM_DEMO`, `EN_COMM_99`, `FR_COMM_99`, `KR_COMM_99`,
      `FR_Mont_99`). This is the session-picker page's button/title text, not a task's
      parameters — needs its own table if it's going into the database at all. —
      **Resolved 2026-07-08: new `session_chooser_configs` table** (`config_name` VARCHAR,
      `language` CHAR(2), `config_json` JSON, `UNIQUE(config_name, language)`), added to
      `CreateSchema.sql` as Table 7. Kept as a JSON blob per row (unlike `ui_labels`) since each
      config's `List` array has a genuinely different shape row to row. `generate_sql.js` now
      routes all 7 registrations there instead of the manual-review list; `Migrated_TaskConfig.sql`
      regenerated with 7 `INSERT IGNORE INTO session_chooser_configs` statements. Side finding
      while migrating: `FR_Mont_99` is registered under an `FR` key but its actual text is
      English — migrated as-is, flagged in `DiscussionLog.md`, not silently corrected. Not yet
      applied to a live database (Part 2 setup).

- [x] **Confirm the 7 pre-existing source bugs are safe to leave as-is (or fix them).**
      Each was an `add('key', ...)` callback in the *current* `.js` files referencing a variable
      that was never defined anywhere in that file — dead code, would have thrown if ever
      selected. — **Resolved 2026-07-08: Jason asked to remove them.** Deleted the 7 broken
      `add(...)` lines directly from source (not just from the migration output):
      `config/DigitSpan_Setup.js` (`DigitSpan_Audio_Backward_Fixed01_Load03`),
      `config/Fluency_Setup.js` (`Fluency_TEST`), `config/ImageCopy_Setup.js`
      (`ImageCopy_FaCE`), `config/SerialSubtraction_Setup.js` (`SerialSubtract_FaCE`,
      `SerialSubtract_FaCE_02`), `config/Stroop_Setup.js` (`Stroop_001`),
      `config/TrailMaking_Setup.js` (`TrailMakingB_DefaultTEST`). Re-ran `extract.js` +
      `generate_sql.js` to confirm: `MANUAL_REVIEW.md`'s "Pre-existing bugs" section is now
      empty. `Migrated_TaskConfig.sql` unaffected (227/139/7 INSERTs unchanged — these
      registrations never produced valid data to begin with).

- [ ] **Figure out what `SelfReport_Setup.js` is.** Jason, 2026-07-08: "ignore the file
      SelfReport_Setup.js. I am not sure what that is yet." All 7 of its registrations (`stai`,
      `nfc6`, `nfc10`, `nfcs`, `panas`, `rtq`, `dass`) reference undefined variables — the whole
      file is effectively non-functional as it stands, and its purpose is unclear (name suggests
      standardized self-report questionnaires, e.g. State-Trait Anxiety Inventory for `stai`,
      Need for Cognition for `nfc6`/`nfc10`, PANAS for `panas` — unconfirmed). `generate_sql.js`
      now skips this file entirely (`SKIP_FILES` constant) rather than reporting it as 7 bugs —
      see `migrate/MANUAL_REVIEW.md`'s "Files explicitly deferred" section. Once its purpose is
      known, either fix the broken `add()` registrations and remove it from `SKIP_FILES`, or
      confirm it's dead and move it to `config/unused/` instead.

---

## Part 2 — Setup & deployment (mechanical steps, on the server)

Full detail for each of these is in `SetupInstructions.md`.

- [ ] Install MySQL and run `mysql_secure_installation` (§1.1)
- [ ] Create the `ncmuser` database user (§1.3)
- [ ] Run `CreateSchema.sql` to create the database and 8 tables (§1.4)
- [ ] Run `SeedData_TaskTypes.sql` to load the 34 task types + Word Recall example (§1.5)
- [ ] Run `migrate/Migrated_TaskConfig.sql` to load the 227 auto-extracted parameter/instruction
      sets + 139 `ui_labels` rows (EN/FR/KR) + 7 `session_chooser_configs` rows —
      **do this after Part 1's decisions are made**, since resolving those may add rows
      to `task_types` first or change what's in the SQL file
- [ ] Insert battery test record (battery_index 16) per §1.6, to have something to query
- [ ] Install Apache + PHP with `pdo_mysql` (§2.1)
- [ ] Deploy `API_battery.php` and `API_task_config.php` to the server (§2.2)
- [ ] Edit DB credentials (`DB_USER`/`DB_PASS`) in both PHP files (§2.3)
- [ ] Test both API endpoints with `curl` (§2.4)
- [ ] Update `DB_API_URL` in `html/JATOS/WordRecallDatabaseConfig.html` to point at the real
      server (§2.6)
- [ ] Restrict CORS (`Access-Control-Allow-Origin`) to the real JATOS origin before going live
      (§2.7) — currently `*` in both PHP files, fine for local testing only

---

## Part 2b — Admin Write API (NCMBatteryWebsite integration)

Full detail in `admin/AdminSetupInstructions.md`. API built 2026-07-08; NCMBatteryWebsite wired
to call it the same day (Jason: "Do it"). Not yet deployed anywhere — both repos still only
run locally.

- [x] Wire `NCMBatteryWebsite` to call the admin API instead of its old behavior (downloaded a
      `battery.json` file via `localStorage`, no backend calls at all). Added `src/api.js`
      (client for both the public read API and the admin write API), `src/AuthContext.js` +
      `src/Login.js` (login gate wrapping the whole app), and rewrote `Home.js`/`FormSettings.js`
      to use them. `npm run build` succeeds (verified 2026-07-08).
- [x] Point `NCMBatteryWebsite`'s task/parameter lists at the live database instead of the
      frozen `src/configFiles/` copy — added a new public endpoint, `API_list_config.php`
      (task types don't have a "list" endpoint yet, only "fetch one specific named thing"), and
      deleted `src/configFiles/` entirely since nothing references it anymore. Instruction-set
      listing is *not* wired up — see gap below.
- [x] Added `API_admin_whoami.php` (not in the original design) so the React app can recover
      login state and a fresh CSRF token after a page reload, since the in-memory CSRF token
      doesn't survive a refresh.
- [x] Found and fixed a latent bug in `NCMBatteryWebsite` while touching this code: `uuid` is
      imported in `App.js`/`Home.js` but was never listed in `package.json` — it only resolved
      because some other dependency happened to hoist it into `node_modules`. Added it as an
      explicit dependency.
- [x] Fixed `NCMBatteryWebsite/README.md` — it was a corrupted UTF-16 file containing only the
      repo name; replaced with real setup instructions (env vars, login flow, current scope).
- [ ] **New gap found while wiring this up: no instructions-set picker.** The DB schema and
      admin API support attaching an `instruction_id` to a `battery_tasks` row, but the original
      UI never had any instructions concept — `FormSettings.js` only ever edited "settings"
      (parameters). Rather than invent unrequested UI, batteries are currently saved with
      `instruction_id` left `null` (the schema allows this). Needs its own design pass if
      instructions should be pickable from this UI.
- [ ] Run `php admin/create_admin_user.php <username>` on the server to create the first
      login (no self-registration by design — privileged lab staff only)
- [ ] Edit `DB_HOST`/`DB_NAME`/`DB_USER`/`DB_PASS` in `admin/admin_common.php` and
      `admin/create_admin_user.php`
- [ ] **Set `ADMIN_ALLOWED_ORIGIN` in `admin/admin_common.php`** to NCMBatteryWebsite's real
      deployed URL once it has one — currently a placeholder
      (`https://your-admin-site.example.edu`). Cannot be `*`; these requests carry a session
      cookie and browsers reject wildcard CORS on credentialed requests.
- [ ] Deploy over HTTPS — the session cookie is marked `secure`, so login will silently fail
      over plain HTTP
- [ ] Deploy `admin_common.php` + the 6 `API_admin_*.php` files together; keep
      `create_admin_user.php` off any web-accessible path (it self-blocks HTTP execution, but
      don't rely on that alone). Deploy `API_list_config.php` alongside the existing public API.
- [ ] Create `user-website/.env.local` with `REACT_APP_API_BASE` / `REACT_APP_ADMIN_API_BASE`
      pointing at the deployed PHP endpoints — see `NCMBatteryWebsite/README.md`
- [ ] Before handling real lab data from outside your own machine, add the items listed under
      "Known gaps" in `admin/AdminSetupInstructions.md`: login rate limiting, an audit log
      (`created_by` column), and update/delete endpoints (deliberately not built yet — this UI
      can currently only create new parameter sets/batteries, never edit or remove one)

---

## Part 3 — Remaining migration work (future design/build steps)

- [ ] Apply Part 1's decisions to `MANUAL_REVIEW.md`'s open items, regenerate
      `Migrated_TaskConfig.sql` if `task_types` changes
- [ ] Design and migrate word lists (currently loaded via `pseudoSwitch` from JS files —
      no schema exists for these yet, this is still an open design question)
- [ ] Design and migrate battery records: convert all entries in `Batteries/Batteries.js`
      (2394 lines) into `batteries` + `battery_tasks` rows — only battery_index 16 exists so far
      (from the §1.6 test insert)
- [ ] Update the remaining task HTML files to fetch config from the database — only
      `WordRecallDatabaseConfig.html` does this today; every other task in `html/JATOS/` still
      loads from `config/*.js` / `InstructionsAndStimuli/Instructions/*.js` directly
- [ ] **Make `ui_labels` the live source instead of `config/General_Setup.js`.** Right now the
      `ui_labels` table is a mirror with no consumer — every HTML page still gets `LabelNames`
      from `config/General_Setup.js` via `pseudoSwitch(LANG+'_LabelNames')` (see `AddNewLabel.md`
      / `AddNewLanguage.md` for the current split). To actually replace it:
      - Add a `ui_labels` endpoint (e.g. `API_ui_labels.php`, modeled on `API_task_config.php`)
        that returns all labels for a given `language` as one JSON object (reassembled from the
        normalized rows — `SELECT label_key, label_value FROM ui_labels WHERE language = ?`).
      - Update each task HTML page to `fetch()` that endpoint and populate `LabelNames` instead
        of relying on `pseudoSwitch(LANG+'_LabelNames')` / loading `config/General_Setup.js`.
      - Since ~50 HTML pages currently call `pseudoSwitch(LANG+'_LabelNames')` (see
        `grep -rl "_LabelNames" html/JATOS/`), this is likely worth a shared helper/include
        rather than editing each page's fetch logic separately.
      - Once every page is switched over, `config/General_Setup.js`'s `LabelNames` blocks become
        dead code and can be removed — don't remove them before then.
- [ ] **Known gap, deferred (not blocking):** `FR` and `KR` `ui_labels` are both missing an
      `Info` key that `EN` has. Jason: left for whenever translation work happens. Query to
      find it (and any future gaps) when ready:
      `SELECT label_key FROM ui_labels WHERE language='EN' AND label_key NOT IN (SELECT label_key FROM ui_labels WHERE language='FR');`
